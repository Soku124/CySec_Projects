# Network Scanner

A simple Python-based network scanner to discover live hosts on a network.
It Broadcasts ARP to get the all live hosts on a network.

# Usage

sudo python main.py -t ip / ip range # require root user

